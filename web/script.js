// Script básico para garantir que o vídeo comece automaticamente
window.onload = function() {
    var video = document.getElementById('videoPlayer');
    video.play(); // Garante que o vídeo inicie automaticamente
};
